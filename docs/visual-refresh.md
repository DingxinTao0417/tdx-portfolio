# 视觉更新记录 · 2026-09-08

## 视觉约定

- 保留暖白、橙色和首页粒子；浅色背景 `#f8f6f2`，正文 `#24221f`，强调色 `#c94720`。暗色使用暖黑与亮橙。
- 内页主标题约 40–64px，段落标题约 28–44px。中文强调词使用正体，英文保留衬线斜体。
- 通用分区使用 `section-space`（手机上下 64px，桌面 96px）。面板圆角 16px，截图 6–8px，输入框 10px。
- 项目使用真实截图、原始宽高比；不通过 `object-cover` 或大圆角裁切界面。首页与项目列表采用一主两次的布局。
- 博客正文限制阅读宽度；移动端提供折叠目录。头像、校徽、项目与文章内容保持原有来源。
- 去除重复首页 CTA、全局跟随鼠标圆环与大面积入场模糊。共享 MotionConfig 尊重系统减少动效设置。
- 技能球按视口适配视野，投影标签避让：小屏最多 7 个，桌面最多 16 个；完整技能仍在目录中。

## 验证记录

- `npm run lint`、`npm run build`、`git diff --check` 通过。构建生成 35 个静态页面。
- 开发服务检查 18 个路由，生产服务检查 15 个中英文路由：均返回 HTTP 200，且各有一个 h1。
- 实际浏览器检查：首页、技能、关于、联系、项目详情、项目筛选、博客列表与正文；覆盖默认桌面视口、1024×768、820×620、390×844，以及浅色与暗色。
- 验证手机表单空输入提示、博客目录展开、移动菜单关闭及短屏滚动。未发送真实表单消息。
- 修复桌面缩到手机时技能球容器撑宽的问题，以及 Lenis 拦截移动菜单内部滚动的问题。
- 首页粒子着色器与状态切换、数据文案、路由、API、依赖版本及部署配置未改动。Three.js 的既有 Clock 弃用提示未在本轮升级依赖处理。

## 交付状态

本地完成，尚未提交、推送或部署。日常预览使用 `http://localhost:3000/`；3001 仅用于临时生产模式检查，检查后停止。

## 首页粒子形状调整（后续）

- 粒子依次组成名字缩写 `TDX`、带 `</>` 的代码窗口、四层圆柱数据库。图形下方保留“TDX / 代码 / 数据库”标签与暂停按钮。没有星球、光环或流程演示面板。
- `hero-targets.ts` 按笔画长度采样并做空间排序；桌面使用 7,000 点，小屏或低核心设备使用 4,200 点。目标位置只在挂载时生成，形变由着色器完成。
- 每种形状停留 4.8 秒，形变最长 2.4 秒。DPR 在挂载时固定；后台、离屏与手动暂停保留进度。系统减少动效设置下显示静态数据库。
- TDX 使用手工几何笔画。数据库包含顶面点阵、侧壁和上下边沿，经 X 轴俯视旋转后保留真实深度；着色器让前侧清晰、后侧变淡，圆柱边缘使用橙色强调。
- 本轮在桌面及 390×844 手机视口确认 TDX、代码窗口、数据库轮廓，手机无横向溢出；数据库加密上下边缘，避免各层难以分辨。形变数学检查覆盖完整过程及极限视差，未发现裁切。
- 浅色、深色与 390×844 手机视口已在浏览器检查，中英文均无横向溢出。暂停与主题切换已验证。静态与数学测试覆盖形变端点、粒子边界和暂停恢复；未进行真实低端设备帧率测试。
- 清除本次误解需求时创建的 `hero-workflow.tsx`、对应 CSS 和 `Home.demo` 文案，不涉及用户截图或项目数据。既有技能球保持不变。

## 首页 AI 粒子模型（2026-09-08，后续已将大脑替换为数据库）

- 当前序列替换为 `TDX → 大脑 → 神经网络`，中英文标签、无障碍描述和无 WebGL 时的占位同步更新。T 保持橙色，D/X 统一使用主题中性色。
- 大脑由双半球曲面、中央纵裂、弯曲脑沟及沟侧回脊构成，保留小脑干和少量内部纤维。回脊按弧长采样，独立法线、透明度和粒子尺寸用于保留表面细节。
- 神经网络以粒子球表现节点，隐藏层在空间中展开；连接采用稀疏路径，橙色信号沿输入至输出方向传播。
- 两个模型保持真实 Z 深度并缓慢转动；字母仅保留轻微鼠标视差。表面法线与深度控制明暗，避免背面粒子覆盖前侧细节。
- 桌面使用 11,000 粒子，窄屏或低核心设备使用 7,200 粒子，DPR 上限 1.5；目标数据在挂载时生成，逐帧变形与信号计算在单个粒子着色器中进行。
- 每种造型停留 6.8 秒、形变最长 2.4 秒。点击模型名称可以选择模型；播放中先完成当前形变，暂停时立即显示所选模型。减少动效时默认显示静态大脑，也可选择其他静态模型。
- 本轮验证：`npm run lint`、`npm run build`（35 个静态页面）和独立 TypeScript 检查通过。真实浏览器覆盖 1440×900、390×844、中英文、深浅主题、手动选型、暂停与恢复；窄屏未发现横向溢出。
- 数值验证覆盖 7 档粒子数、位置/样式/法线重排一致性、自动播放与减少动效状态机，以及形变过程和极限视角的投影边界。网络视差限制为 ±0.015、自动转角限制为 ±0.02；272px 画布下，计入抖动和高亮粒子足迹后的最小节点间距约 3.36px。未进行实体低端设备帧率测试。

## 恢复多层数据库（2026-09-08）

- 按用户要求移除大脑造型，当前序列为 `TDX → 数据库 → 神经网络`。数据库恢复 `1b28ee2` 中的四层圆柱、顶面点阵、侧壁和橙色边沿，保留原有层间距与俯视角度。
- 保留当前 TDX、神经网络、粒子数量、点击选型和暂停功能；减少动效时默认显示静态数据库。只对数据库单独收细粒子，并恢复前后深度淡化。
- 移除无引用的 `hero-brain.ts`，可从 Git 历史恢复；中英文标签、无障碍说明与无 WebGL 降级图标同步更新。
- 验证：lint、生产构建（35 页）、差异检查通过。9 档粒子数的 45 次字节比较确认数据库坐标与历史一致，TDX、网络与 seeds 未改变。浏览器检查桌面、390px 宽度、深浅主题、暂停与选型，未发现横向溢出或控制台错误。
- 本次修改仅在本地，未提交、推送或部署。预览服务保留在 3000 端口。

## 三层数据库与早期层叠形态（2026-09-08）

- 数据库调整为三层，每层厚度由 0.26 增至 0.44，中心间距改为 0.76；保留层间空隙，增加侧壁采样以表现厚度。
- 从 `5f9c1a2` 找回早期 `lattice`，作为第四个形态“层叠 / Layers”。保留六层方形薄片、原始俯视角、0.06 连续 simplex 噪声起伏及轻微慢摆；没有恢复星球、鼠标排斥、滚动散开或动态 DPR。
- 当前顺序：`TDX → 数据库 → 神经网络 → 层叠`。保留点击选型、暂停和减少动效支持，窄屏收起编号并允许控制栏换行。
- 点数适配当前 7,200 / 11,000 点；10 档点数的数据检查通过，完整方格点数时层叠坐标与历史逐位一致，TDX 与神经网络数据未改变。
- lint、生产构建（35 页）与差异检查通过。浏览器验证三层厚度、第四形态、手动切换与循环、深浅主题，以及 390px 中英文布局；未见横向溢出或控制台错误。未进行实体低端设备帧率测试。
- 本轮仍为本地修改，未提交、推送或部署；3000 端口服务继续运行。

## 实心网络节点与数据库层次（2026-09-08）

- 神经网络节点由透明球壳、轨道和中心核改为均匀填充的粒子圆盘，保留很浅的球冠深度；增加覆盖密度，使节点呈实心效果。节点布局、半径与连线坐标不变。
- 数据库仍为三层，厚度增至 0.5、中心间距增至 1.05，俯视角为 0.35。只保留前侧壁和前下弧，每层仅上边缘使用橙色强调，消除背面透线造成的额外圆环。
- 为数据库增加独立的透明度、颜色和点径属性，和位置一起空间排序；不改变粒子总数、循环顺序或其他形态。
- 浏览器检查桌面与 390px 视口、深浅主题、暂停选型和形变；实心节点与三个厚圆柱可辨，无横向溢出或控制台错误。lint、生产构建（35 页）及差异检查通过。
- 数值检查覆盖属性长度、有限性、确定性及联合排序；7200 / 11000 点的数据库静态与相关形变保守投影检查未发现截边。未进行实体低端设备帧率测试。
- 仅本地修改，未提交、推送或部署。临时验证标签页已关闭，3000 端口开发服务保留。

## 轮播顺序与画面占比（2026-09-08）

- 轮播改为 `TDX → 神经网络 → 数据库 → 层叠`：名字开场，网络与数据居中，舒展的薄片收尾。保留着色器模型 ID，播放顺序独立配置。
- 移除底部编号、模型名称及手动选型逻辑；右下角保留 44px 暂停按钮与键盘焦点提示。中英文无障碍描述及降级图标顺序同步更新。
- 扩大实际画布而非缩窄相机视野：手机宽度增加 10%、平板 14%、桌面 22%；桌面同时略增文案间距，整体造型较上轮约大 20%。画布围绕原区域居中展开，避免增加整段首屏高度，并保留形变边界余量。
- lint、生产构建（35 页）通过；实际帧回调测试确认两轮顺序、暂停与离屏冻结、恢复首帧忽略后台时间，以及减少动效时静态数据库。浏览器检查 1270px、1024px 和 390px 布局，无横向溢出。
- 未改粒子数量、DPR、三层数据库或实心网络节点几何；未提交、推送或部署。3000 端口开发服务保留。

## 继续放大粒子（2026-09-08）

- 桌面画布在上一轮基础上再放大 30%（列宽的 122% → 158.6%），中心略向左移；外框改为正方形，为上下轮廓留空间。窄桌面简介收窄，避免文字靠近形变路径。
- 手机按屏幕边界将画布限制为列宽的 130%，中心左移 12px；800px 以上单列使用 148.2%。手机没有强行放满额外 30%，以保留网络最右侧节点及过渡粒子。
- 点径根据画布宽度和粒子数量同比适配，防止放大后的实心节点变稀；模型几何、粒子数、DPR 与播放顺序未改变。
- 浏览器确认桌面与 390px 布局、实心节点及无横向溢出；控制台无错误。定向 ESLint、生产构建（35 页）通过。修改仍仅本地，未提交或推送。

## 粒子交互（2026-09-08）

- 点击或轻触粒子区域切换到下一个形态，顺序仍为 `TDX → 神经网络 → 数据库 → 层叠`，不恢复底部编号和模型名称。键盘聚焦后可用 Enter / Space 切换，焦点提示收在底部小提示内，不用巨大的画布描边。
- 第一次鼠标进入或聚焦时显示约 3 秒的轻提示；键盘操作时保留焦点提示。拖动超过 8px 不触发切换，不拦截页面原生滚动。
- 指针靠近网络节点时，该节点变亮，信号沿与它直接相连的路径向外传播；离开后渐隐。节点拾取只检查 18 个投影中心，输入使用 ref，逐帧颜色和信号计算留在着色器中。
- 把轮播抽成独立状态机：形变中点击会等当前过渡完成，连续点击合并为一个待执行切换。操作后约 4 秒内不自动换形；暂停或系统减少动效时，手动切换直接显示静态目标，不启动动画。
- lint、生产构建（35 页）、差异检查通过。状态机检查覆盖自动循环、连续点击、暂停恢复、减少动效以及 20,000 帧混合事件；网络位置与样式数据保持不变，新拓扑属性与原属性联合排序。
- 浏览器检查中英文、键盘切换、暂停后切换、拖动防误触、节点局部高亮，以及 390×844 窄屏滚动和布局；无横向溢出或控制台错误。未进行实体手机触摸或低端设备帧率测试。
- 仅本地修改，未提交、推送或部署；3000 端口开发服务保留。
