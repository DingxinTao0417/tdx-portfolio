# 视觉更新记录 · 2026-09-08

## 视觉约定

- 保留暖白、橙色和首页粒子；浅色背景 `#f8f6f2`，正文 `#24221f`，强调色 `#c94720`。暗色使用暖黑与亮橙。
- 内页主标题约 40–64px，段落标题约 28–44px。中文强调词使用正体，英文保留衬线斜体。
- 通用分区使用 `section-space`（手机上下 64px，桌面 96px）。面板圆角 16px，截图 6–8px，输入框 10px。
- 项目使用真实截图、原始宽高比；不通过 `object-cover` 或大圆角裁切界面。首页与项目列表采用一主两次的布局。
- 博客正文限制阅读宽度；移动端提供折叠目录。头像、校徽、项目与文章内容保持原有来源。
- 去除重复首页 CTA、全局跟随鼠标圆环与大面积入场模糊。共享 MotionConfig 尊重系统减少动效设置。
- 技能球按视口适配视野，投影标签避让：小屏最多 7 个，桌面最多 16 个；完整技能仍在目录中。

## 验证记录

- `npm run lint`、`npm run build`、`git diff --check` 通过。构建生成 35 个静态页面。
- 开发服务检查 18 个路由，生产服务检查 15 个中英文路由：均返回 HTTP 200，且各有一个 h1。
- 实际浏览器检查：首页、技能、关于、联系、项目详情、项目筛选、博客列表与正文；覆盖默认桌面视口、1024×768、820×620、390×844，以及浅色与暗色。
- 验证手机表单空输入提示、博客目录展开、移动菜单关闭及短屏滚动。未发送真实表单消息。
- 修复桌面缩到手机时技能球容器撑宽的问题，以及 Lenis 拦截移动菜单内部滚动的问题。
- 首页粒子着色器与状态切换、数据文案、路由、API、依赖版本及部署配置未改动。Three.js 的既有 Clock 弃用提示未在本轮升级依赖处理。

## 交付状态

本地完成，尚未提交、推送或部署。日常预览使用 `http://localhost:3000/`；3001 仅用于临时生产模式检查，检查后停止。

## 首页粒子形状调整（后续）

- 粒子依次组成名字缩写 `TDX`、带 `</>` 的代码窗口、四层圆柱数据库。图形下方保留“TDX / 代码 / 数据库”标签与暂停按钮。没有星球、光环或流程演示面板。
- `hero-targets.ts` 按笔画长度采样并做空间排序；桌面使用 7,000 点，小屏或低核心设备使用 4,200 点。目标位置只在挂载时生成，形变由着色器完成。
- 每种形状停留 4.8 秒，形变最长 2.4 秒。DPR 在挂载时固定；后台、离屏与手动暂停保留进度。系统减少动效设置下显示静态数据库。
- TDX 使用手工几何笔画。数据库包含顶面点阵、侧壁和上下边沿，经 X 轴俯视旋转后保留真实深度；着色器让前侧清晰、后侧变淡，圆柱边缘使用橙色强调。
- 本轮在桌面及 390×844 手机视口确认 TDX、代码窗口、数据库轮廓，手机无横向溢出；数据库加密上下边缘，避免各层难以分辨。形变数学检查覆盖完整过程及极限视差，未发现裁切。
- 浅色、深色与 390×844 手机视口已在浏览器检查，中英文均无横向溢出。暂停与主题切换已验证。静态与数学测试覆盖形变端点、粒子边界和暂停恢复；未进行真实低端设备帧率测试。
- 清除本次误解需求时创建的 `hero-workflow.tsx`、对应 CSS 和 `Home.demo` 文案，不涉及用户截图或项目数据。既有技能球保持不变。
